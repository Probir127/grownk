## This repository has no Python dependencies.
# The previous content was human-readable bullets and caused `pip install`
# to fail because lines starting with '-' are not valid requirements.
# Leave this file empty or add valid pip requirements like:
# requests==2.31.0
